---
const { isRed = false } = Astro.props;
---

<div
    class="transition-colors items-center space-x-1 hover:cursor-pointer hover:bg-blue-600 rounded px-2 inline-flex bg-blue-500 text-white"
    class:list={{"bg-red-500 hover:bg-red-600": isRed}};
>
    <slot name="start-icon" />
    <slot name="text">
        <span>
            Soy un boton 
        </span>
    </slot>
    <slot name="end-icon" />
</div>
